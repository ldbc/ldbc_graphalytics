# Properties file describing the benchmark

# Include benchmark run properties file, which defines a subset of
# graphs and algorithms to run
include = runs/all.properties

# Include other properties files
include = graphs.properties
